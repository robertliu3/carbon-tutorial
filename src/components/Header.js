
//for now leave it as the navbar on top, will change to corbon element later
function NavHeader() {
    return (
        <header className="titleName">
            Github Repository List form IBM
        </header>
    );
}

export default NavHeader;